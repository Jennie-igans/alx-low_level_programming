#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - prints program that prints the alphabet in lowercase, followed by a new line
 * Return: Always 0.
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	return (0);
}
